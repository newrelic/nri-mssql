name: com.newrelic.mssql
description: Reports status and metrics for MSSQL service. Legacy file kept for compatibility.
protocol_version: 1
os: windows

commands:
  all_data:
    command:
      - .\bin\nri-mssql.exe
    prefix: config/mssql
    interval: 15
